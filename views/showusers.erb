<%= @results%>

<table class="table">
	<% @results.each do |row| %>
	<tr>
		<td><%= row['username'] %></td>
		<td><%= row['datetime'] %></td>
		<td><%= row['phone'] %></td>
		<td><%= row['color'] %></td>
	</tr>
	<%end%>
</table>